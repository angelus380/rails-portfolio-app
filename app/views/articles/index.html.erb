<p id="notice"><%= notice %></p>

<h1>Articles</h1>
<% @articles.each do |article| %>
  <div><%= link_to "#{article.title} - #{article.created_at.strftime("%D")}", article_path(article) %></div>
<% end %>
